pip3 install prisma_sase

Update the sec_prefix.csv header row with your local prefix names then set per site values, site needs to be active for bindings

python3 local_sec_pref_csv_uploader.py

Use at own risk :)
